import { HslToStringPipe } from './hsl-to-string.pipe';

describe('HslToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new HslToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
